@using BaseballUa.Data;
@model BaseballUa.ViewModels.VideoVM

@{
    ViewData["Title"] = "Add Video";
    SelectList categorySL = ViewBag.CategorySL;
    SelectList newsSL = ViewBag.NewsSL;
    SelectList teamSL = ViewBag.TeamSL;
    SelectList gameSL = ViewBag.GameSL;
    SelectList sportSL = ViewBag.SportSL;
}

<div class="container-fluid text-warning adminhead">
    <div class="mb-0 mt-2 mb-5 p-0">
        @await Html.PartialAsync("_AdminHead")
    </div>
</div>

<h1>Додати відео</h1>

<hr />
<div class="row m-2">
    <div class="col-md-4">
        <form asp-action="AddVideo" asp-route-navNewsId="@ViewBag.newsId" asp-route-navGameId="@ViewBag.gameId" asp-route-navTeamId="@ViewBag.teamId">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="SportType" class="control-label"></label>
                @Html.DropDownListFor(c => Model.SportType, sportSL, optionLabel: "--- оберіть ---", htmlAttributes : new { @class = "form-control"})
                @Html.ValidationMessageFor(c => c.SportType, "", new { @class = "text-danger" })
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsGeneral" /> @Html.DisplayNameFor(model => model.IsGeneral)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <input asp-for="Description" class="form-control" />
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Fname" class="control-label"></label>
                <label>(URL адреса, наприклад: https://youtube.com/something)</label>
                <input asp-for="Fname" class="form-control" />
                <span asp-validation-for="Fname" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PublishDate" class="control-label"></label>
                <input asp-for="PublishDate" class="form-control" />
                <span asp-validation-for="PublishDate" class="text-danger"></span>
            </div>
            <div class="form-group m-1">
                <label asp-for="NewsId" class="control-label"></label>
                @Html.DropDownListFor(c => Model.NewsId, newsSL, optionLabel: "--- оберіть ---", htmlAttributes : new { @class = "form-control"})
                @Html.ValidationMessageFor(c => c.NewsId, "", new { @class = "text-danger" })
            </div>
            <div class="form-group m-1">
                <label asp-for="CategoryId" class="control-label"></label>
                @Html.DropDownListFor(c => Model.CategoryId, categorySL, optionLabel: "--- оберіть ---", htmlAttributes : new { @class = "form-control"})
                @Html.ValidationMessageFor(c => c.CategoryId, "", new { @class = "text-danger" })
            </div>
            <div class="form-group m-1">
                <label asp-for="TeamId" class="control-label"></label>
                @Html.DropDownListFor(c => Model.TeamId, teamSL, optionLabel: "--- оберіть ---", htmlAttributes : new { @class = "form-control"})
                @Html.ValidationMessageFor(c => c.TeamId, "", new { @class = "text-danger" })
            </div>
            <div class="form-group m-1">
                <label asp-for="GameId" class="control-label"></label>
                @Html.DropDownListFor(c => Model.GameId, gameSL, optionLabel: "--- оберіть ---", htmlAttributes : new { @class = "form-control"})
                @Html.ValidationMessageFor(c => c.GameId, "", new { @class = "text-danger" })
            </div>
            <div class="form-group m-1">
                <input type="submit" value="Додати" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

